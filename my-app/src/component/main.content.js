import React from 'react'

const styles = {
    main: {
        display: "flex",
        height: "80vh",
        justifyContent: "center",
        alignItems: "center",
    }
}

function Main(){
    return (
        <main style = {styles.main}>
            main
        </main>
    )
}

export default Main;