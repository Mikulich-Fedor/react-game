import React from 'react'

const styles = {
    footer: {
        display: "flex",
        height: "10vh",
        justifyContent: "center",
        alignItems: "center",
        marginTop: "auto"
    }
}

function Footer(){
    return (
        <footer style = {styles.footer}>
            footer
        </footer>
    )
}

export default Footer;