import React from 'react'

const styles = {
    head: {
        display: "flex",
        height: "10vh",
        justifyContent: "center",
        alignItems: "center",
    }
}

export default function Header(){
    return(
        <header style = {styles.head} >
            hi
        </header>
    )
}